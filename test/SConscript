# -*- python -*-

tools = Split("""
pentek
qtt_qtconfig
boost_date_time
boost_program_options
doxygen
logx
""")

sources = Split("""
capture.cpp
dactest.cpp
dacdump.cpp
toggle_716x_leds.cpp
""")

headers = Split("""
""")

env = Environment(tools = ['default'] + tools)

html = env.Apidocs(sources + headers)

capture = env.Program('capture', 'capture.cpp')

dactest = env.Program('dactest', 'dactest.cpp')

dacdump = env.Program('dacdump', 'dacdump.cpp')

toggle_716x_leds = env.Program('toggle_716x_leds', 'toggle_716x_leds.cpp')

# TODO dacdump does not currently compile!
#Default(capture, dactest, dacdump, toggle_7142_leds, html)
Default(capture, dactest, toggle_716x_leds, html)

